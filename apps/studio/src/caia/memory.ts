// Re-export the lib implementation so imports like "@/caia/memory"
// resolve to the real functions during builds.
export * from "../../lib/caia/memory";

export default undefined as unknown as any;
