import type { OwnerPolicy } from "./policy";

export const ownerGlobalPolicy: OwnerPolicy = {
  id: "owner-global-150",
  name: "Owner Global 150% Logic",
  decisionMode: "EXECUTE",
  allowOptions: false,
  maxClarifications: 1
};
