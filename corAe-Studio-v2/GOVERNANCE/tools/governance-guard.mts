// governance-guard.mts — lightweight guard helpers
export function evaluateInputGovernance(message: string) {
  // placeholder: real evaluation logic lives in tools; this is canonical location
  return { ok: true, verdict: 'neutral', reason: 'placeholder' };
}
